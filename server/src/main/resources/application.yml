spring:
  application:
    name: smart-parking-system
  datasource:
    url: jdbc:postgresql://localhost:5432/parking_db
    username: parking_user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
